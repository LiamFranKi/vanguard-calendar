services:
  # Solo Frontend (Estático - 100% GRATIS)
  - type: web
    name: vanguard-calendar-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://api.example.com
